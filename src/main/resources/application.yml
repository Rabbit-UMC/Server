spring:
  datasource:
    url: ${url}
    username: ${username}
    password: ${password}
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

#  루나
  redis:
    host: localhost
    port: 6379

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace
